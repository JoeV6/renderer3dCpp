
/*
#include "camera.h"
#include "fps.h"
#include "math.h"

using namespace std;

class Physics {
	Camera& camera;
	vector<Mesh&> &meshes;

public: 

	Physics(Camera& camera, vector<Mesh&>& meshes) : camera(camera), meshes(meshes) {

	}

	void update() {

	}
};

class PhysicsObject {
	Mesh& mesh;
	Vec3d position;
	bool collidable = true;

public:

	bool isCollidable() {
		return collidable;
	}

	void collide(Mesh* mesh) {
		
	}

	bool isColliding(Mesh* mesh) {
		
	}
};
*/